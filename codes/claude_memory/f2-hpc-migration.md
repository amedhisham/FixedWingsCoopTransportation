---
name: f2-hpc-migration
description: "F2 HPC migration plan (parallel collection DONE) — remaining steps: Ubuntu venv, Linux FMU re-export, SLURM. Concrete code-side TODOs (2/3/4) staged, doing one at a time."
metadata: 
  node_type: memory
  type: project
  originSessionId: e49cf5f7-ad57-4237-adbd-51d78f2ea4a7
  modified: 2026-09-09T19:44:09.694Z
---

**GOAL: move F2 MAPPO training to uni HPC for real speedup.** The parallel-collection prereq is DONE (2026-09-09): `parallel_collect.py` (multiprocessing Pool, per-worker FMU env, actor-only `collect_chunk`, value batched in `main`), `NUM_WORKERS` flag in mappo.py. See [[f2-slide-fix-todo]] for the diagram that went stale from that refactor. INFRA context in [[f2-residual-rl-plan]] ("build worker-pool as SLURM prereq" — now built).

**MEASURED (6-core laptop, STEPS_PER_ITER~32k):** iter time by workers: 2w 80s, 3w 70s, 4w 55s, 5w 50s, 6w 43s. Fit `iter ≈ 25 + 111/n` -> **serial floor ~25s (the PPO UPDATE, doesn't parallelize), 1-worker collection ~111s.** Still scaling at 6w -> HPC (16-32 cores) keeps helping toward the ~25s floor (~1.5x more from 6->32). Value-batching refactor also sped BOTH paths (old seq was 195s). Timing split now in the log: `| Xs (coll Y upd Z)`. The 3w dip = WHOLE-EPISODE ROUNDING (episodes 1800 steps) -> on HPC pick STEPS_PER_ITER divisible by `n_workers x 1800`.

**GPU VERDICT FLIPPED (2026-09-10, see [[f2-noise-limited]]):** the "CPU is optimal" call below assumed we were update-bound at modest batches. WRONG for the real problem — the gradient-noise-scale diagnostic proved F2 is DEEPLY noise-limited (critical batch ~MILLIONS of steps vs 92k working). 2 trajectories already need millions/iter; a trajectory DISTRIBUTION needs far more -> **thousands of parallel collectors -> GPU-tensorized plant is now the PLAN, not optional.** CPU multiprocessing (even 192 cores) tops out an order of magnitude short. The GPU-plant port is plant-port-only (no in-loop IPOPT; base = frozen F1 net — see GPU-PLANT FEASIBILITY below), ~1 week with the user guiding the physics. The reasoning below stands ONLY for the small-batch regime we've now outgrown. **INTERIM (before GPU): run the 192-core node as a BIG-BATCH CPU run — 192 workers × ~11.5k ≈ 2.2M steps/iter at ~the SAME collection wall-time as the laptop's 92k (per-core FMU rate fixed) -> lands at the critical batch -> likely solves the current 2-traj on CPU (GPU is for the trajectory DISTRIBUTION). Full code changes staged in `HPC_MIGRATION.md` Step 5: NUM_WORKERS<-SLURM_CPUS_PER_TASK, STEPS_PER_ITER = NUM_WORKERS*EPISODES_PER_WORKER*EPISODE_STEPS (~2M), EPOCHS 8->2 (big-batch, tames the serial update ~10min->~2min), optional MINIBATCH 512->4096 + LR 3e-4->6e-4, BLAS/headless hygiene. Expect critB near/below batch + gcos off ~0 = escaped noise floor; ignore first ~20 iters.**

**GPU / Isaac verdict (reasoned through 2026-09-09; SUPERSEDED for scale — see flip above):** DON'T GPU the update — nets are TINY (256x256) so gradient steps are kernel-launch-STARVED on GPU (slower than CPU BLAS); the CPU<->GPU transfer is once/iter and amortized, NOT the issue. GPU only pays if the SIM goes on GPU (Isaac-style thousands of parallel envs) — but that needs REIMPLEMENTING the FMU plant as GPU tensors (throws away validated physics). AND at CURRENT modest steps/iter you're ~UPDATE-BOUND once cores are plentiful, so GPU sim saves only the residual collection (~marginal). GPU win unlocks ONLY at million-step batches (where update also grows + matmuls saturate the GPU). Verdict: **CPU multiprocessing is optimal for a CPU/FMU plant at current scale.**

**CLUSTER NODES (2026-09-09):** best node for F2 = `runner` 192 CPU cores (8x Xeon 8260) / 6TB — collection is CPU/FMU-bound so MAX CORES wins; GPU nodes (gpu5-6 = 64c+10x A40; gpu4 = 32c+5x3090+3x A40; runner-04-06 = 72c+P2000) are WORSE for this job (fewer cores + GPUs your workload can't feed). Episode granularity CAP: workers useful only up to STEPS_PER_ITER/1800 (at 32k -> ~16 cores max before a worker gets 0 episodes); to USE 192 cores you must scale STEPS_PER_ITER to ~n_workers*1800 (=345k at 192, ~10x current), which pushes you into big-batch where the UPDATE (serial, ~6s@32k, grows w/ batch) becomes the floor. Efficient point = ~16 cores @ STEPS_PER_ITER=28800 (16*1800): iter ~13s (coll ~7 + upd ~6) vs laptop 40s. Laptop HT wall measured: 8 workers = peak (~+10% over 6, 90-100% CPU), 10 REGRESSES to 6-level (oversubscription; 6 physical cores). MUST pin BLAS at high core counts (OMP/OPENBLAS/MKL_NUM_THREADS=1).

**GPU-PLANT FEASIBILITY — CORRECTED (2026-09-09, user caught my error):** I claimed the CasADi/IPOPT expert was an in-loop "wall" for a GPU port. WRONG for F2. VERIFIED in code: residual_marl_env.py:6 + :305 "no CasADi anywhere" — each drone runs a LocalModelAgent = the FROZEN F1 DISTILLED NET, not the optimizer (that's the whole point of F1 distillation). IPOPT (`expert_path`) runs ONLY in mappo.build_pairs (main, ONCE) to generate the trajectory library (expert_lib.npz); the per-step "expert" is a PRECOMPUTED array indexed by phase (mappo.py:275) + shipped to workers as dpos_list. So a GPU/tensorized-plant port is PLANT-PORT-ONLY: FMU dynamics+integrator -> batched tensors (MEDIUM, laborious: recover eqs from .c/model, watch STIFF cable constraint for integrator choice) + serious VALIDATION vs FMU (the gate). Everything else is already GPU-friendly (F1 base net + allocation pinv; residual actor/critic; reward = elementwise; ref library = gather; domain-rand = noise+gather). NO expert-redesign research risk. Feasible bounded engineering, NOT a research gamble. STILL don't do it now (not variance-limited, 192 cores = zero-risk speedup); revisit ONLY if scope outgrows 192 cores (thousands of parallel envs).

**Extra parallel refinements DONE (2026-09-09, in git, user commits):** (a) ship dpos arrays to workers via `build_pairs(dpos_list)` -> NO per-worker CasADi rebuild (was `N_workers x len(MIX_DIRS)` redundant IPOPT rollouts absorbed into iter-1 = the ~97s iter-1); (b) CLEAN Ctrl-C: workers `signal.SIG_IGN`, `ParallelCollector.terminate()` force-kills without join()-hang, pool creation inside the try (old code FROZE the terminal on Ctrl-C -> recover: `taskkill /F /IM python.exe`); **REAL root cause found 2026-09-10 (WINDOWS-only): the FMU's Intel-Fortran runtime installs a console Ctrl-C handler that `abort()`s the process (`forrtl: error 200`) BEFORE Python's KeyboardInterrupt/SIG_IGN can run (one forrtl-200 per FMU process, main+workers) -> FIX = `os.environ.setdefault("FOR_DISABLE_CONSOLE_CTRL_HANDLER","1")` at the TOP of BOTH mappo.py and parallel_collect.py (+ in _init) before the DLL loads. SIG_IGN/terminate alone were NOT enough; this env var is the actual fix. NO-OP on Linux (Ctrl-C reaches Python normally) — keep the line, don't remove it;** (c) coll/upd timing split. Portable memory + `HPC_MIGRATION.md` + `requirements.txt` committed into `codes/claude_memory/` + repo so a fresh Ubuntu Claude has context (RL branch ahead of origin, user pushes).

**Migration is being done ONE STEP AT A TIME (user's call).** Blockers the user named: (1) rebuild venv on Ubuntu, (2) re-export FMU with Linux support, (3) SLURM.

**STEP 1 — venv on Ubuntu: DONE the prep.** `codes/requirements.txt` generated via pip freeze (45 pkgs). CAVEAT: `torch==2.11.0+cu130` is a CUDA local-tag wheel that won't resolve on plain PyPI/Linux — install torch separately on Ubuntu (CPU build is fine; training is DEVICE="cpu", CPU-bound). Rest (casadi/IPOPT, FMPy, numpy, scipy, gymnasium, pettingzoo, matplotlib) are cross-platform. PIN casadi/IPOPT to the validated version so expert trajectories don't shift.

**STEP 2 (SAVED) — compile the FMU for Linux, NO re-export.** Inspected `Base_Model.fmu` (the one `fmu_plant_env` loads by default) 2026-09-09: binaries = `win64/*.dll` ONLY (won't load on Linux), BUT source is FULLY included (`sources/*.c` + `buildDescription.xml`; Simulink/Embedded Coder RTWCG -> portable self-contained C, no MATLAB runtime). So on Ubuntu just COMPILE it: `sudo apt install build-essential` then `python -m fmpy compile Base_Model.fmu` (adds `binaries/linux64/*.so`, same filename, no code change). Verify: `python -c "import fmpy; fmpy.dump('Base_Model.fmu')"`. Fallback only if compile hits missing symbols: re-export from Simulink with Linux binaries. (three_drones fmu = same, but code uses Base_Model.fmu.)

**STEP 3 (SAVED) — SLURM.** parallel_collect is SINGLE-NODE multiprocessing (shared memory, NOT multi-node) -> request `--nodes=1 --cpus-per-task=<many>`, one fat node. Make `NUM_WORKERS` fall back to `os.environ["SLURM_CPUS_PER_TASK"]` so no per-alloc hand-edit. KEEP mp context = "spawn" (NOT fork: main creates its own FMU env before the Pool; fork would copy that live FMU/IPOPT handle into workers = native-state corruption; spawn re-imports cleanly).

**STEP 4 (SAVED) — HPC run hygiene (batch script `train.slurm`).**
- BLAS OVERSUBSCRIPTION footgun: each worker's numpy/torch BLAS spins a full node-sized thread pool -> N workers x N threads = N^2, can be SLOWER than serial. Set `export OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1`. (torch.set_num_threads(1) is already pinned per worker in parallel_collect, but the BLAS env vars are separate.)
- HEADLESS matplotlib: `main()` ends with `plt.show()` + builds figures -> errors/hangs on a headless node. Set `MPLBACKEND=Agg` and switch final `plt.show()` -> `savefig`. Small guard, no-op on Windows.
- All of 3/4's code changes are no-ops on the current Windows run (safe to add anytime before travel to HPC).
