---
name: f2-mix-curriculum
description: F2 RUNG-3 MIX curriculum (multi-direction joint training) + the critic 76-D desired-load-state fix + the not-conflict-just-slow diagnosis. Current active work as of 2026-09-09.
metadata: 
  node_type: memory
  type: project
  originSessionId: e49cf5f7-ad57-4237-adbd-51d78f2ea4a7
  modified: 2026-09-09T19:44:51.044Z
---

**RUNG 3 = MIX: train JOINTLY over movement DIRECTIONS (the generalization vehicle).** `MIX_DIRS` in mappo.py, quintic per direction (`make_quintic_pose`, scale `MIX_SCALE`=5m, ramp 16s, horizon `OVERFIT_END`=HOLD+16+1~18s). Builds on [[f2-distillation-capacity-verdict]] (map is REPRESENTABLE) + [[f2-axis-generalization]].

**CRITIC INPUT GREW 58 -> 76 (2026-09-09).** `critic_input()` now appends the 18-D DESIRED LOAD STATE `[p_d, R_d(9), v_d, ω_d]` (via `get_reference_trajectory(env.t, env.traj)`), so state = true(42) + delays(4) + carrier-tgt(12) + desired-load(18). WHY: carrier targets ALONE do NOT determine the load reference — cable-suspended load, so carrier->load is forward-kinematics with a SWING DOF (underdetermined); the critic was inferring p_d from the formation only via the expert's placement convention. Now it gets p_d directly (paired with true load in state) = clean reference side of the load-tracking penalty. Training-only/privileged, actor obs UNCHANGED at 98. Warm-start REINITS the critic (state_dim changed) -> brief EV dip then recovers. (On big multi-traj mixes this matters more; on the easy 2-traj it barely moved EV.)

**THE 5-MIX "not learning / high variance" DIAGNOSIS (decisive chain):**
- NOT critic-identifiability: **EV stable 0.92-0.98** (advantages CLEAN, low-noise). Ruled out.
- NOT the dlam CLIP dead-zone: **satL/satW sub-cap** (below 1). Ruled out (that dead-zone = [[f2-shear-precursor-rewards]]/overshoot term; would zero the dlam gradient if saturated).
- SO the bottleneck is the POLICY UPDATE side, not the critic. Two suspects: (A) multi-task GRADIENT CONFLICT, (B) just SLOW + high stochastic variance.
- **2-TRAJ CONFLICT PROBE** ({+x+y+z, +x+y-z} — same +x+y, only z-sign flips): they **CO-LEARN (not anti-phase)** -> NOT gradient conflict. So the 5-mix failure is BREADTH/VARIANCE + genuine off-axis slowness (RUNG-1 single +y took 150+80 iters), COMPOUNDED by batch-splitting (each traj gets ~half the per-iter gradient).
- FIX = CURRICULUM: solve ONE direction on the FULL batch first (no splitting), save, then re-add the 2nd WARM-STARTED (they co-learn -> 2nd comes up fast). Currently doing +x+y+z solo -> then add +x+y-z. Expand to 5 only once 2 co-learn cleanly (add one dir at a time = rehearsal, avoids the sequential-forgetting seen in [[f2-overfit-adapt-experiment]]).

**POSITIVE TRANSFER OBSERVED (2026-09-10, curriculum working):** single-traj +x+y+z plateaued DET_R ~-0.42 / loop ~0.47. Warm-started the 2-traj phase (+x+y+z, +x+y-z, 92k steps, log_std floor on) -> by iter 100 the 2-traj net BEAT the 1-traj net ON THE SHARED TRAJ by ALL metrics: DET_R -0.42->-0.39, loop 0.47->0.435 (per-traj [+x+y 0.437, +x+y 0.433] = the ADDED traj reached PARITY), load 0.087 (better), EV 1.00, ent 3.55 (floor holding). So adding a related traj REGULARIZES the shared net toward the common solution -> better generalization AND better on the original. First EMPIRICAL confirmation (not just distillation-predicted) that breadth = performance, not a tax = the empirical backbone of "expand the mix bit by bit". CONFOUND (user acknowledged): 3 things changed at once — log_std floor(sigma>=0.25), STEPS_PER_ITER 32k->92k, the 2nd traj. Clean disambiguation (PENDING): rerun single +x+y+z under the NEW settings (floor+92k); still ~0.47 => delta is transfer, drops to ~0.435 => win was floor/steps (2nd traj "free" not helping). Either way a keeper.

**SPEED LEVERS (clean advantages -> trust the direction, take bigger steps):** LR_ACTOR is the lever (tried 3e-4->6e-4). BUT 6e-4 accelerated ENTROPY COLLAPSE (4.1->2.5 fast). ENT_COEF small (0..2e-3). Note entropy dip-then-RISE-to-~5 (with ENT_COEF~0) = policy still finding better actions in the tails (mean mis-placed, not stuck) — only bad if it RUNS AWAY or DET_R stalls while it inflates. DEPLOYED policy is the MEAN (DET_R), so entropy=exploration-std matters only via collection quality/runaway. If premature collapse: log_std FLOOR (clamp min after opt step) decouples mean-speed from std-collapse; if runaway: log_std CEILING. **log_std FLOOR now IMPLEMENTED (2026-09-10, mappo.py): LOG_STD_MIN=log(0.25), `actor.log_std.clamp_(min=...)` after opt_a.step() — applied when single-traj DET_R plateaued while entropy slid 4.0->3.2 (~0.1/iter); mean learns at full LR, std can't collapse. sigma>=0.25 => entropy>~2.6.**

**BIGGER-BATCH LEVER — CORRECTED (2026-09-10, user pushed back, I was WRONG):** I'd said "bigger batch doesn't help, EV high = low variance = wrong lever". WRONG. EV measures the CRITIC's fit (per-sample regression R^2) — it is BLIND to the POLICY-GRADIENT variance (the score-fn term, which states/tasks landed in the batch, action sampling). High EV + a NOISY policy gradient are fully consistent -> that's exactly the "trending-right-but-noisy DET_R" seen on 1-2 trajs, and 5-mix "cooked". Multi-task DILUTION (32k split K ways -> each task's gradient from 32k/K samples) IS gradient noise from poor sampling -> MORE STEPS directly reduces it. The single-traj-learns-on-32k / 5-mix-fails-on-32k evidence does NOT prove "steps don't help" — single puts all 32k on one task, 5-mix ~6.4k each (5x fewer per task). Co-learning (no conflict) means the true multi-task gradient is GOOD -> the ONLY barrier is estimating it = variance = steps help. CAVEAT: variance ~1/sqrt(N) (diminishing) -> at high K brute-force steps get costly => curriculum (100% batch on one, warm-add) is the cheaper route to the SAME root cause (per-task sample count); use both. SCALING LAW for joint: STEPS_PER_ITER ~ K * per_task_budget (hold per-task density). The ONLY no-help case = +y BLOWUP reachability (action never sampled, [[f2-axis-generalization]]) — coverage of the ACTION space, not gradient variance. (Acted on it: STEPS_PER_ITER 32k->92k for the 2-traj phase.)

**TOOLING added this session (in git):** scale_test.py — loopMSE/loadMSE columns (m²) + on-blowup prints the EPISODE-MEAN-until-blowup stats (loop/load/MSE) instead of nothing; determinism confirmed (seeded EVAL_SEED, mean policy) — irreproducible old figures were config drift (EVAL_DELAYS [1,2,2,1]->[2,2,2,2] on Aug 28, imported from mappo). deploy_compare.py — QUINTIC MOVE mode (dial dir/mag/ramp like scale_test) + load MSE / carrier-vs-optimizer MSE / lambda-vs-opt MSE table + tracking-error & carrier-deviation time plots + move_descr titles (dir·mag not label). mappo.py — coll/upd timing split. Parallel-collection infra: see [[f2-hpc-migration]].

**TOOLING — TRUE RESUME (RESUME_OPT, 2026-09-13, mappo.py).** save_ckpt now takes `save_opt`/`it_done`; the `*_last.pt` save (normal END + the Ctrl-C handler) writes **Adam moments (opt_a/opt_c) + iter count** on top of weights/critic/norm (log_std was already inside state_dict). `*_best` stays LEAN (deploy/warm-start artifact — no opt bloat). New flag **RESUME_OPT** (near WARMSTART): True + WARMSTART=a *_last.pt -> restores Adam moments + iter count + KEEPS the saved annealed log_std (guards the `log_std.fill_(LOG_STD_INIT)` re-init via `resuming`), loop numbers continue (`range(start_iter+1, start_iter+ITERS+1)`, ITERS = MORE iters). False (default) = cold Adam from WARMSTART weights. WHY: catch an END-STREAK — a run still descending at the ITERS cap (this one WAS) or Ctrl-C'd mid-improvement can now continue with momentum instead of throwing away Adam's second-moment estimates. Backward-compat: an old *_last.pt with no opt state + RESUME_OPT=True -> warns + falls back to cold Adam (works from the NEXT run's *_last onward). NOTE: THIS run's continuation is a NO-streak tail (best iter148 -0.426 > last iter150 -0.475) -> correct call is RESUME_OPT=False cold-from-best (residual_mappo_overfit.pt); RESUME_OPT pays off next time there's an end-streak.

**RESULT — 3-TRAJ RUN DONE (2026-09-13, log f2_4961590.log, 150 iters, best net -> residual_mappo.pt).** ALL FOUR WATCHES GREEN + a real optimizer finding. DET_R **-0.593 -> -0.426** (best iter 148) = a **0.167** gain, ~14x the ENTIRE 2-traj optimizer squeeze (0.012 in [[f2-rotation-limited]]) => EMPIRICALLY: **breadth is the big lever, the optimizer was not** (confirms the [[f2-rotation-limited]] "the big lever is FORMULATION not optimizer" caveat). Per-traj loop @best: **+.2x-y-z 0.713->0.469** (brave new dir LEARNED, dropped hard), **+x+y+z 0.435->0.431** and **+x+y-z 0.442->0.442** (both known dirs HELD, zero forgetting — +x+y+z even nudged better = the 3rd task REGULARIZED the shared net, same positive-transfer signature as the 1->2 traj step above). blowups sporadic-single (iter 15/138/149/150), not systematic. **THE OPTIMIZER FINDING (answers the open "spiral worse or cleaner?" from the KICKOFF reads): the 3-traj field descended CLEANER, less rotation than 2-traj.** R held steady **~0.65** the whole run (2-traj DECAYED 0.84->0.34 = the spiral); **gcos@8 stayed POSITIVE ~0.4-0.7** (2-traj went ->0/negative at lag 8 = the ~8-iter rotation period) => the rotation LARGELY VANISHED. Mechanism: adding a genuinely off-axis 3rd task BROKE the 2-player rotational symmetry that caused the orbit in [[f2-rotation-limited]] (non-conservative field -> more conservative with a 3rd, differently-coupled task). gnorm even GREW late (0.15->0.24) with rising gcos + DET_R still improving at iter 150 => **NOT converged, the run was cut short by ITERS=150, more headroom.** Entropy slid 4.15->2.80, hit the sigma=0.32 floor exactly at the end (floor held, no collapse). Transient: iter 132 DET_R -0.885/coord 0.493 = one bad eval, recovered next iter (not persistent). NEXT: (a) CONTINUE this run (warm from residual_mappo_last.pt, more iters — it was still descending); (b) add a 4th dir (breadth pays, and it straightens the field); (c) thesis-worthy write-up: "adding tasks REDUCES the rotational pathology" is a clean, non-obvious result. The stale KICKOFF reads/caveats below are now ANSWERED (norm did NOT need re-estimating — +.2x-y-z dropped fine on the frozen 2-traj norm).

**RESULT — 3-TRAJ CONTINUATION = SATURATED, DO NOT RE-CONTINUE (2026-09-13, log f2_4961990.log, 150 iters).** Cold-Adam restart from the -0.426 best (RESUME_OPT=False, ent pumped fresh to 4.008), SAME config (3 dirs @ 5m/16s, horizon 18s — NOT the scale run). Best net of the ENTIRE run = **DET_R -0.421 at ITER 1**; gain over baseline = **0.005, banked immediately**; iters 4-150 drifted WORSE (-0.43..-0.50, gap stayed negative -0.02..-0.05, two eval blowups -0.87 @108/128 both on +.2x-y-z), ent collapsed 4.0->2.80, final -0.435. DECISIVE READ: a *fresh exploration restart* probed the whole neighborhood for 150 iters and found NOTHING better than its start, then re-collapsed = the task is SATURATED, not plateaued-through-able. This is the empirical bookend to the RESULT block above — the 3-traj-at-5m net gave 0.167 the FIRST time (breadth) and 0.005 the second time (re-squeeze) => **breadth is the lever, re-continuing a solved config is ~zero EV** (also re-confirms the RESUME_OPT note's call: this was a no-streak tail, cold-from-best was correct, and it STILL found nothing => the ceiling is the task, not the optimizer state). +.2x-y-z remains the sole pain (loop 0.47-0.65, owns both blowups) but from its FLOOR, not undertraining. CONSEQUENCE: next signal must come from a NEW/harder task -> the [[f2-speed-binding-axis]] SCALE run (base-blow scales 10/29,10/29,18/52), NOT another same-config continuation. Best net now lives in residual_mappo_overfit.pt (-0.421) = the scale run's warm-start. LESSON for the scale run: watch first ~30 iters; if it stalls like this, KILL EARLY (don't pay the full 150 to re-measure saturation).

**RUNG-3 EXTENDED TO 3-TRAJ — GENERALIZATION KICKOFF (2026-09-13, running on HPC).** After [[f2-rotation-limited]] settled that the 2-traj optimizer is basin-capped ~-0.38 (that's the A->B LINE-best + where PPO's rotational spiral stopped — NOT a proven global cap; a better optimizer may reach lower, OPEN question) and that reward-shaping is largely exhausted (leak_w dead: coord metric blind to F_null; dw-consistency delay-capped at consist 0.11, didn't move loop; relative-drone-pos = COMMS, violates the no-comms thesis — all in [[f2-wd-nullleak-breakthrough]]), pivoted to GENERALIZATION (breadth, its own axis, not blocked by the perception wall). Added a 3rd, deliberately BRAVE off-axis dir **+.2x-y-z = (0.2,-1,-1)** (mostly -y-z, UNSEEN; -y is the historically hard axis [[f2-axis-generalization]]) to MIX_DIRS alongside +x+y+z / +x+y-z. CONFIG: WARMSTART=**residual_mappo_overfit_interp075.pt** (the -0.380 free net from the [[f2-rotation-limited]] interpolation), **FREEZE_LOG_STD=False** (learnable sigma back on, floored sigma=0.32), ENT_COEF=0 (the log_std FLOOR is the exploration-preservation mechanism, not the entropy bonus), STEPS_PER_ITER=1.4M (~467k/task, SUPERCRITICAL per task since critB~300k), ITERS=150, MIX_SCALE=5. **PRE-FLIGHT (deterministic eval of the -0.38 warm-start on the 3-traj mix, laptop):** NO BLOWUP on the new dir. (NOTE 2026-09-13: the authoritative warm-start baseline is what mappo prints at run start = **DET_R -0.593** over the 3 MIX_DIRS with interp075's frozen 2-traj norm; the earlier laptop pre-flight number "-0.7217" was a config-drift artifact — the pre-flight script lived in scratchpad and is gone, but committed mappo has CONSIST_W=CONSIST_LAM_W=0 / EVAL_DELAYS=[2,2,2,2], so the laptop run had a different penalty weight or delay set. -0.593 is BETTER, use it; per-traj loops below should be unchanged if only the reward scalar shifted.) (feasibility-curriculum requirement met — task must be survivable), per-traj loop +x+y+z **0.434** / +x+y-z **0.438** (the 2 known INTACT from warm-start) / +.2x-y-z **0.759** (survivable but poorly tracked = real learning headroom). READS for the run: (1) +.2x-y-z loop DROPPING from 0.759 = learning the new dir; (2) the 2 known HOLDING ~0.44 = no catastrophic forgetting; (3) blowups~0; (4) R/gcos = does a hard 3rd traj spiral WORSE or descend cleaner? CAVEAT: warm-start uses the 2-traj obs-NORM (interp075's, NOT re-estimated over 3 trajs) — survived pre-flight, but if +.2x-y-z loop refuses to drop, RE-ESTIMATE the norm (the norm bug that bit before: line-only estimate_norm -> desired-state dims blow up). Diagnostics (gcos multi-lag, R, gnorm) left ON. Tooling for the analysis path: interp_search.py + save_interp.py (checkpoint interpolation + free-net save), linesearch.py/.slurm.
