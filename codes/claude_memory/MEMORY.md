# Project Memory Index

- [Project Overview](project_overview.md) — Fixed-wing cooperative cable-suspended load transport, based on paper p2025ut + thesis Girardello_Sofia
- [User Profile](user_profile.md) — Master's student implementing a cooperative transport system
- [F1 Milestone](f1-milestone.md) — F1/pR_dot base proven on ONE trajectory (IL+clock+DAgger); desync-robustness arc (low-pass reconstruct_lp, noisy DAgger, ANALYTIC flag); + GENERALIZED to quintic 6D-pose trajectory DISTRIBUTION (trajectories.py, collect/dagger/deploy multi-traj, env-threaded): held-out tracks CM-level (generalizes!), but closed-loop BUZZ plateaus in the static-hold phase -> likely MEMORY/capacity, not coverage
- [F1 Anti-memorization Plan](f1-antimemorization-plan.md) — BUZZ RESOLVED (in-loop EMA = warm-start continuity; imitation can't stabilize a gain≈1 loop); optimizer A/xi ratchet RESOLVED (deadband: skip solve when min analytic ||v_Ri|| > 0.3); clock = positional encoding -> IMPLEMENTED (2026-08-14): +5 custom HORIZONTAL solver-engaging trajectories (z doesn't engage — internal-force coord is a horizontal-plane phenomenon), T_END 25->35s, randomized quintic move-end ~U(20,30)s. PENDING the retrain run
- [F1 Curate Redesign](f1-curate-redesign.md) — DONE: DAgger train-new-FULLY + sample-old restructure (MAX_AGG 250k pool, TRAIN_BUDGET 200k; ~126k new rows/iter; killed the train-on-whole-150k / evict-fresh-data disaster). TODO: rebuild curation deterministic — guaranteed bin quotas + SEPARATE hardness/recency filters, hardest-wins (never recency-priority)
- [F1 DAgger Warm-start Gate](f1-dagger-warmstart-gate.md) — warm-start TRANSMITS a jittery net's bad weight-basin (MSE<->buzz strong-but-imperfect; slack is where jitter hides) -> DAgger GATES on buzz: stays at a beta until mean buzz<pass, warm-starts retries from last-good, keeps ALL data when stuck (only beta<=0.1). + REFINEMENTS (STUCK_AFTER=0, PASS-trains, evict-on-advance). RESUME/SAVE BUG FIXED 2026-08-18 (saved `policy` not the verified net -> "dagger plot clean, deploy noisy"; proven buzz 0.18 vs 0.016; save `recent[-1]` instead) + CONTINUOUS AUTOSAVE of last-2 verified-good (_last/_prev every accept, any exit). vmin=0 is a t=0 red herring; gate stays buzz-only
- [F1 Phase Slip + Lambda History](f1-phase-slip-lambda-history.md) — RESOLVED 2026-08-18: F1 works closed-loop (deploy-verified). Was a SUDDEN phase slip into ANTI-PHASE lock (feedback lambda->-lambda degeneracy; buzz kicks it), root cause = memoryless net vs STATEFUL A/xi-ratcheting expert (POMDP). FIX = LOG-SPACED lambda-history taps [1,2,4,8,16,32,64,128,192,256] steps (~one period, straddles the ~1.5s flip; dense 0.1s window was too short) + the save-bug fix. Aux-A/xi / phase-supervision / self-conditioning discussed, NOT needed. NOW ON F2
- [F2 Training Log](f2-training-log.md) — ORDERED log of two-head residual training regimes + HARD-WON LESSONS: exploration-trap (clip reward terms on exploration-inflated quantities), reward-balance (keep terms within 1 order of magnitude), entropy tuning (ENT_COEF 0.003), swing carried by LOAD term (not coord/jerk). Current working config + regime 4 (surpass expert) next
- [F2 Loiter Reward](f2-loiter-reward.md) — replace expert-PATH tracking (which forces stall: the reference dips to ~0.2 at turns) with "ORBIT your moving attachment at speed floor V_ORBIT ≥ eps+load_speed" -> stall geometrically impossible. Diagnosis: mid-flight stall, IDENTICAL turns differ -> state/desync-dependent not structural; reward-tuning exhausted (manifold_w=0 still stalls -> it's optimization/blowup-cliff not reward); actuator-obs rejected (LLC unknown). Terms: radius + one-sided circulation + radial, manifold_w=0, warm gt2
- [F2 Estimation Dead-End](f2-estimation-dead-end.md) — KF-in-front-of-base experiment (kf_probe.py) PROVES estimation can't fix F2 coord: generous KF WORSENS loop (1.35-1.5 vs raw 1.1 vs perfect 0.013), robust across 12 tunings + corr 0.995/0.98. Coordination ≡ agreement ≡ shared signal; per-drone independent filtering DIVERGES -> decentralized estimation can't manufacture agreement. Residual MUST be robust CONTROL. De-risks the RL thesis. + 2 FOLLOW-UP probes (refbase_probe/refbase_split_probe): feed-ref-to-everything coordinates (coord=0) but load open-loop EXPLODES; surgical split (w_d measured, lambda shared) halves leak but LOOP unchanged ~1.1 even w/ PERFECT lambda -> loop is bottlenecked by DIVERGENT w_d not lambda. REFINEMENT: 2 failures/2 sources (leak<-lambda, loop-scatter<-w_d); classical BIND = measured-w_d scatters drones vs reference-w_d loses load feedback, mutually exclusive -> RL navigates the coord-vs-track tension (3 classical baselines each wall, RL owns the gap)
- [F2 w_d Null-Leak Breakthrough](f2-wd-nullleak-breakthrough.md) — THE mechanistic resolution (2026-08-26): drone scatter = w_d DISAGREEMENT leaking range->null(G) as F_null (measured 0.22N, ||G@F_null||=5e-16 invisible to load, 3-5x the coord metric which is BLIND to it); RL patches with asymmetric dlam -> leaks BACK to load (whack-a-mole = the whole coord-vs-track frontier). FIX = dw-consistency LOSS (regress dw head -> dw*=w_clean-w_base, the SHARED true wrench; probed FITS cap_w, dlam* is a no-op bc lambda already coordinated). WIRED in mappo (CONSIST_W=0.1, track_clean_lambda). Loss not reward (pathwise vs score-fn); scaffold breaks the coordination-discovery barrier, RL owns hedged control on the delay-limited residual. "Hinges on the load estimate" = problem statement, WHY it's an RL thesis (KF can't fix it)
- [F2 Axis Generalization](f2-axis-generalization.md) — F2 residual x-SPECIALIZED, +y BLOWS. **MAJOR UPDATE 2026-08-29: CAPACITY CLOSED, diagnosis = EXPLORATION/DODGE.** Sweeps run (direction_sweep 26 sign-grid: only ±x clean, coverage UNCORRELATED w/ failure; library_sweep: 12/50 blow IN-SAMPLE, ALL have +y, +y-DOMINANCE not magnitude). NOT capacity: base tracks +x HARDER than +y (1.390 vs 0.925) so +x is LEARNED specialization not task-ease, net does harder+x -> can represent easier+y; widen 128->256 + cold (256,128,64) both failed; residual DESTROYS a stable +y base. NOT credit-assignment (huge dense penalty, 0 progress). WALL = +y is a DODGEABLE minority in joint training (penalty says "don't" not "do instead"; net has no better +y action in experience — every +y attempt blows). FIX = inject +y POSITIVE experience: curriculum or specialist-distill. Depth (residual blocks) discussed+set aside (function-preserving inherits stuck basin; capacity not the wall; z breaks SO(2))
- [F2 Distillation Capacity Verdict](f2-distillation-capacity-verdict.md) — **DECISIVE 2026-09-01: the joint-direction wall is PPO OPTIMIZATION, not capacity.** distill_test.py: roll each single-task expert (ch_y +y, customxy_ch5 +x+y) on its traj -> harvest (obs->mean action) -> supervised-fit ONE net to both. One net fits BOTH R^2~0.99 (256), gap SHRINKS 256->512 (2.99->2.06) = NOT capacity. Aliasing already out (reference in obs -> separable). Joint RL failed via the incentive trap (blowups late -> more punishment -> early-death gradient), not representability. FIX = seed RL from a BC net (not a bigger net); confirm by BC joint net + closed-loop roll on +y AND +x+y. Tooling: flush=True added; fits could use GPU (cuda avail; harvest is FMU/CPU)
- [F2 Overfit Adapt Experiment](f2-overfit-adapt-experiment.md) — became a FEASIBILITY CURRICULUM (2026-08-31..09-01). Incentive redesign (reachable no-blowup outcome + un-froze dlam head: cap_lam 0.5->0.65, overshoot_w 1->10). RUNG 1 (+y quintic ramp16, 19s) SOLVED loop~0.24 (150+80 iters, +x forgot ~0.01); RUNG 2 (+x+y custom 35s) SOLVED but COOKED +x/+y quintics = SEQUENTIAL single-task forgetting (no rehearsal); JOINT failed via incentive trap -> looked like capacity BUT distillation proved it's PPO ([[f2-distillation-capacity-verdict]]). Also widened old-arch residual_mappo_r4base -> _wide.pt (obs 44->98, hidden 128->256, function-preserving). eval_policy blowup KeyError fixed
- [F2 Shear Precursor Rewards](f2-shear-precursor-rewards.md) — blowup = pure formation SHEAR (drones rip apart, COM+load stay put, LOAD-INVISIBLE so coord/leak blind; recurrent excursions ~10s then ~0.2s cliff). Two precursors: OVERSPEED (direct, drone speed>3, w1/cap75/lin2 — v_mean~0 so speed~=shear) + OVERSHOOT (indirect, un-saturate dlam, sat>1, w1/lin1/cap9 — ZERO below cap so no authority loss, discourages whack-a-mole). satL/satW in DET print. DELAY FIX: uniform 0-1-2 walk, ceiling 2 all drones train=eval ([2,2,2,2]). Tooling: direction_sweep.py, library_sweep.py, scale_test USE_CUSTOM (lib load, no IPOPT). loop needs per-drone ref rollout, load is free from traj; IPOPT is deadband-gated (cost is FMU rollout)
- [F2 Mix Curriculum](f2-mix-curriculum.md) — RUNG-3 multi-direction JOINT training + critic 58->76 (desired-load-state, carrier-targets don't fix load ref = cable swing DOF) + DECISIVE diagnosis: 5-mix "not learning" is NOT critic (EV 0.92-0.98) NOT dead-zone (sat sub-cap) NOT gradient-conflict (2-traj z-flip CO-LEARN) -> it's breadth/variance+off-axis-slowness+batch-splitting -> FIX = solve-one-then-add curriculum. Entropy/LR levers. Session tooling (scale_test MSE+pre-blowup stats, deploy_compare MSE/move-mode, timing split)
- [F2 Noise-Limited](f2-noise-limited.md) — **DECISIVE 2026-09-10: F2 residual RL is gradient-NOISE-LIMITED, critical batch ~MILLIONS of steps vs 92k working (settles "need more steps?" with a number; user's instinct right, EV blind to policy-grad noise).** Diagnostics in mappo.py: critB (gradient noise scale, McCandlish arXiv:1812.06162, NS=8 CONTIGUOUS split) + gcos (cross-iter cosine). IGNORE first ~20 iters (EMA warmup + log_std transient inflates gcos 0.63->0). raw nan = signal below noise floor. 2-traj needs millions/iter -> generalizing needs FAR more -> **GPU-tensorized plant (thousands of collectors) NOW NECESSARY, flips the CPU-optimal verdict**
- [F2 HPC Migration](f2-hpc-migration.md) — parallel collection DONE + MEASURED (2w80 3w70 4w55 5w50 6w43s; fit iter~25+111/n, floor=~25s UPDATE serial; still scaling->HPC helps). GPU verdict: don't (tiny nets kernel-starved; update-bound at current steps; Isaac needs FMU reimplementation). Ship-dpos + clean-Ctrl-C + timing-split done. STEP1 venv (requirements.txt, torch CPU on Linux). SAVED STEP2 FMU (fmpy compile Base_Model.fmu, source bundled), STEP3 SLURM (single-node, NUM_WORKERS<-SLURM_CPUS_PER_TASK, spawn), STEP4 (BLAS *_NUM_THREADS=1, MPLBACKEND=Agg). Portable memory+guide committed to codes/claude_memory
- [F2 Slide Fix TODO](f2-slide-fix-todo.md) — DEFERRED: mappo_residual_overview.pptx top band stale after parallel-collection refactor (Critic moves Phase1->Phase2 since value now batched in main, buffer drops 'value', state 58->76). Bottom env pipeline unchanged
- [F2 Residual RL Plan](f2-residual-rl-plan.md) — MAPPO residual (two-head dlam/dwrench). Multi-traj + CRITIC-TARGET fix (was traj-blind) works but modest; diagnose_f2 3-way decomp: base_clean 0.013 -> desync 1.10 -> residual RECOVERS 70% (loop 0.33) but coord LEAK 7.5x + load 2.4x. WALL = decentralized COORDINATION (POMDP): base coordinates free (identical net/shared load view -> agreeing lambda); residual conditions on OWN state -> MISALIGNED lambda -> leak (bounded forces but load-invisible nullspace drift INTEGRATES to 1.1m; load survives=regulated). Lit: history alone doesn't break the tracking-vs-coord frontier, relative-drone-pos does. USER: push frontier WITHOUT comms. ACTIVE EXPERIMENT (2026-08-23): +desired-load-state OBS(shared ability) + full-wrench leak_w REWARD(CTDE incentive, =0.05 after 1.0 steamroll) + net (256,128,64) cold-start. Just fixed a CRITICAL norm bug (line-only estimate_norm -> desired-state dims ~1e6 on quintics -> "not learning"); RESTART pending. INFRA parked: FMU sim = bottleneck, local MP ~2-3x (6-core), HPC = real 10-30x (build worker-pool as SLURM prereq)
