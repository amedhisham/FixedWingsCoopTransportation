---
name: f2-smoothness-manifold-replacement
description: F2 next reward round — kill the jumpiness/discontinuities (sharp 90° turns, vnorm 1->1.5->1 in <0.1s). User REJECTS load-anchored loiter geometry (re-imports load noise/delay); wants manifold REMOVED, replaced by self-state smoothness + load penalty. Key risk = manifold also pins the load-invisible nullspace.
metadata:
  node_type: memory
  type: project
---

**STATUS: DESIGN SETTLED + CALIBRATED, NOT IMPLEMENTED — DEFERRED until the [[f2-speed-binding-axis]] one-by-one scale-curriculum runs FINISH (user 2026-09-14: "log these, do not implement, we finish the current runs first, i just wanted to line up the test").** All the open decisions below are now nailed and the three deadband thresholds are measured. Do NOT wire until the scale runs are done. Still PAIRED with [[f2-scale-invariant-obs]] as the "next reformulation" pile.

**SETTLED + CALIBRATED DESIGN (2026-09-14) — the reward that replaces manifold point-tracking.** `manifold_w -> 0`; per-step per-drone reward = THREE DEADBAND terms + unchanged `load_w·load_err`; **FREEZE_LOG_STD=True** (mandatory — else the policy cuts the smoothness penalty by COLLAPSING exploration std instead of smoothing the mean = the old jerk-fail below); ramp `smooth_w` in (FIX the thresholds at the separating line, ANNEAL the weight — one knob, not two). dt = env control dt. Warm-start current net, current scale, one-variable curriculum.
1. **LANE (position, replaces the point-pull):** `lane = Σ_axis relu(|p_i − expert_pos[i][step]|_axis − h)²`; reward `-= lane_w·lane`. Center = the SAME time-indexed expert point the env ALREADY computes for `d2` (NOT the attachment-frame `p_d+R_d@Bb[i]` — user: overcomplicated, drop it). Deadband = ZERO inside the box, quadratic outside = freedom to orbit inside a tube that TRANSLATES with the mission (resolves the nullspace pushback below: the moving box pins the load-invisible `F_null` drift the manifold used to pin). **h = 0 is EXACTLY today's point-tracking** (continuity). Start **ISOTROPIC h = 0.10 m**, climb by trial (user's call — hold anisotropy till h is big). NOTE the snaps won't visibly relax until h approaches the LOOP EXTENT (~1.0 m xy / ~0.4 m z, measured) because the center orbits; below that the drone is still mostly outside the box = still pulled. Revisit per-axis (smaller `h_z`) only once h grows to matter.
2. **SMOOTHNESS — acceleration:** `-= smooth_w·relu(|‖v_t‖−‖v_{t-1}‖|/dt − a_thresh)²`. **a_thresh ≈ 4.75 m/s²** (pooled separating line; LENIENT start — expert never exceeds ~1.75 so tighten toward ~2.5 once stable for harder smoothing). Free to accelerate for the ramp; penalize only excess. Well-defined at any speed (no gate needed).
3. **SMOOTHNESS — heading:** `-= smooth_w·relu(angle(v_t,v_{t-1})/dt − w_thresh)²`, **GATED on ‖v‖ > v_floor** (reuse epsilon/stall margin). **w_thresh ≈ 8.8 rad/s** (must CLEAR the expert's genuinely fast orbit, p99 ≈ 4.9 rad/s ~ a real 2 m/s / 1 m-radius loiter — don't drop below ~5). WHY the speed gate: heading = v/‖v‖ is UNDEFINED as ‖v‖→0; dividing a near-zero v by its own tiny magnitude turns a trivial wobble into a spurious ~90° swing -> without the gate the term punishes the policy for slowing down and pollutes the gradient with noise. (User: won't dip below epsilon in cruise anyway, but keep the guard.) Use the 3D velocity direction = covers banking-turns AND climb in one term.

**CALIBRATION — how the 3 thresholds were measured (codes/smoothness_calib.py, NEW 2026-09-14).** Rolls the DEMONSTRATED EXPERT PATH (`expert_path`, differentiated for v = the smooth "good" class, CAN'T blow up — unlike the zero-residual base which blows at these scales) vs the DETERMINISTIC POLICY on the 3 benchmark dirs (`+x+y+z` 10/29, `+x+y-z` 10/29, `+.2x-y-z` 18/52) under eval desync; dumps expert-vs-policy percentiles of accel `|Δ‖v‖|/dt`, heading rate `angle(v_t,v_{t-1})/dt`, and per-axis lane excursion `|p−expert|`. Threshold = geomean separating line (expert p99, policy p90). RESULTS (pooled): **accel** expert p99 **1.75** vs policy p50 **2.88** (policy MEDIAN already beats expert p99 = CLEAN separation), policy tail to 135; **heading** expert p99 **4.9** (real fast orbit) vs policy p90 **15.7**, tail to 227 (bands overlap MORE than accel -> line must sit above ~5); **lane excursion** p90 ~1.0 m xy / ~0.4 m z, p99 ~2–2.7 xy / ~1 z. The policy flew ALL 3 dirs deterministically at their hard scales (no blowup) -> the snaps are genuine in-flight OUTLIERS, not blowups. Clean state pulled from `env.state()` (s[18:18+3n]=drone pos); run locally like scale_test.py (torch/casadi env, not on the default python3 PATH here). Re-run to recalibrate if the warm net changes.

**NULLSPACE PUSHBACK — RESOLVED by the deadband box.** (Earlier concern: remove the manifold and a slow SMOOTH drift through the load-invisible nullspace `F_null` is unpenalized — smoothness kills jitter but not smooth drift, load_err is blind to it, [[f2-wd-nullleak-breakthrough]].) The LANE box translates with `expert_pos` and bounds how far each drone can sit from its reference point per axis -> it BOUNDS the nullspace excursion the way the manifold's point-anchor did, without pulling to a moving point. So box (coordination/nullspace pin) + smoothness (kills jitter) + load_w (the collective objective) cover it. ACCEPTANCE = load stable + drones in-lane + motion smooth (the leak metric stays secondary).

**DECISIONS TO NAIL — NOW ALL RESOLVED (see the SETTLED + CALIBRATED block above; kept here for the reasoning trail):** (1) LANE ANCHOR — box around clean reference attachment `p_d+R_d@Bb[i]` (my recommendation) vs box around expert_pos[i][idx] deadband vs something else; (2) lane SHAPE + SIZE (ball vs axis-aligned box; half-width ~ loiter radius + margin — needs a number); (3) SMOOTHNESS term form — start uncapped frozen-log_std jerk, or go straight to Δ||v|| speed-2nd-diff + heading-rate-change (~0 for a smooth orbit); (4) curriculum: anneal manifold_w->0 rate + smoothness ramp-in; (5) whether to run the cheap diagnostic rollout first to confirm the jumps are manifold-phase-catchup (vs speed-band edges / delay lag). SETTLED: reward = LANE(box) + self-smoothness(frozen log_std) + load + optional leak_w; manifold REMOVED; NO loop-curve, NO noisy-load anchor; acceptance = load stable + drones in lane + smooth motion.

**PROBLEM (2026-09-13, user watching deployed mean-policy vnorm + trajectory):** carriers make violent DISCONTINUITIES — sudden sharp ~90° turns, vnorm jumping 1->1.5->1 in <0.1s. User's read: the carriers snap to match the TIME-INDEXED manifold "at all costs." Confirmed in code: env reward still does time-indexed point tracking `d2 = ||expert_pos[i][self._step] - p_i||^2` with **manifold_w=1.0** ([residual_marl_env.py step]); if the carrier drifts off-phase (desync/delay) the target keeps advancing on the clock -> it CHASES a moving phase-point -> overshoot/sharp-turn/speed-jump. Want instead: "stay in your lane, orbit SMOOTHLY."

**REJECTED — the [[f2-loiter-reward]] design (load-anchored phase-free geometry).** User explicitly rejects it (2026-09-13; wasn't logged as rejected before): the loiter anchor `c_i = npos + nR@Bb[i]` is built from the LOAD STATE, so radius/circulation/radial terms RE-IMPORT the noisy/delayed load -> same jitter source. Do NOT resurrect load-anchored loiter geometry.

**DESIRED DESIGN (user, minimal — "don't impose more stuff on it"):** remove manifold; use the DRONE SELF-STATE ONLY for loitering; impose JUST SMOOTHNESS on the drone's own motion; let the LOAD PENALTY (load_w, the true objective + the ONLY load coupling) make it find the best smooth orbit that keeps load error minimum. Candidate smoothness: the jerk term with **log_std FROZEN** (FREEZE_LOG_STD flag exists) — user notes "still not enough, we need proper replacement of manifold."

**WHY jerk failed before + the fix:** jerk penalty tried 2x, failed ([residual_marl_env.py:212-219]) because exploration inflates jerk ~30x -> cheapest way to cut it is COLLAPSE log_std (kill exploration), not smooth the mean; reward improved via entropy collapse while DET jerk stayed flat, even clipped. FIX (user's instinct, correct): FREEZE log_std -> removes the "shrink std" cheat -> the only lever left is smoothing the MEAN. Two mechanics to get right: (1) UNCAP/raise jerk_cap=0.05 — with std frozen the mean-smoothing gradient is wanted, but if the SAMPLED jerk sits above the cap the gradient is zeroed and the mean never smooths; give headroom. (2) Consider penalizing abrupt CHANGES — (Δ||v||)² speed-2nd-diff + heading-rate change — which are ~0 for a smooth constant-speed orbit and spike exactly on the 1->1.5->1 / sharp-turn artifacts, whereas raw jerk also lightly tolls the legit orbit (centripetal jerk ω²v, small for a slow orbit).

**THE CRITICAL RISK (my pushback, grounded in [[f2-wd-nullleak-breakthrough]]):** the manifold isn't only tracking — it PINS THE NULLSPACE. load_err is BLIND to the coordination scatter (w_d disagreement -> F_null in null(G), ||G@F_null||~0, load-invisible = the whole F2 loop wall). The manifold is a per-drone position anchor INDEPENDENT of the load, so it currently keeps drones OUT of that nullspace. Remove it and lean on load_w+smoothness: cable tethers each drone to its attachment sphere, load_w holds the COLLECTIVE, but WHERE each drone sits in the nullspace/swing DOF is now FREE -> smoothness kills JITTER but not a SMOOTH slow drift through the nullspace. So "load penalty takes care of the load" is TRUE; "takes care of the LOOP" is FALSE (loop = the load-invisible part). Jumps may just be traded for smooth loop/coord degradation.

**(B) phase-free NEAREST-POINT-ON-LOOP = REJECTED by user (2026-09-13, I suggested it, retracted).** User TESTED distance-to-loop BEFORE they added time-indexing; it didn't work -> time-indexing was the fix that came AFTER. Key reasoning: **"coordination without time is not coordination"** — the time-indexed loop IS the coordination mechanism (shared clock -> phase agreement); strip the time and you remove coordination WITHOUT buying real freedom (still a 1D curve = "specific points, just more of them"). Worst of both. (Caveat noted by user: the loop test was EARLY on a trash policy; might behave differently warm now — but they lean against it.)

**THE ACTUAL DESIGN (user, 2026-09-13): "stay in your LANE + orbit smoothly."** The LANE = a bounding BOX / region (a VOLUME, not a curve, not a point): FREEDOM inside, penalty only OUTSIDE. This is what PINS THE NULLSPACE / handles the loop (replaces the manifold's silent anchoring) -> resolves my nullspace pushback. Plus self-state SMOOTHNESS (frozen-log_std jerk / Δ||v||) = orbit smoothly. Plus LOAD penalty = load stability. leak_w = OPTIONAL backup, NOT a primary concern: ACCEPTANCE CRITERION = load stable + drones contained in lanes + smooth motion => "we good" (behavior is the goal, the leak METRIC is secondary). NO time-indexed phase-point, NO loop curve, NO noisy-load anchor.

**LANE ANCHOR (the one open design choice — moving mission A->B means the lane must TRANSLATE with the formation, so it needs a clean anchor):** RECOMMENDED = box around the CLEAN REFERENCE ATTACHMENT `c_i^ref(t) = p_d(t) + R_d(t)@Bb[i]` (p_d,R_d from get_reference_trajectory = precomputed DESIRED pose, NOT the measured noisy load -> the clean cousin of the rejected loiter anchor which used MEASURED pose), half-width ~ loiter radius + margin; penalty `relu(||p_i - c_i^ref|| - R_lane)²` (deadband, zero inside). Non-orbiting center -> no residual phase-chase. Alt (worse): deadband ball around expert_pos[i][idx] but center ORBITS so needs R_lane >= loop diameter or it re-becomes soft phase-chasing. AWAITING user confirm on anchor/shape/size before wiring.

**IMPLEMENTATION (once anchor confirmed):** manifold_w=0; add deadband-LANE term + frozen-log_std SMOOTHNESS term (uncapped jerk to start); keep load_w + stall/overspeed guards; leak_w=0 (backup). Warm-start current net, current scale, one-variable curriculum (anneal in). WATCH: jump-metric (mean-policy Δ||v|| / turn-rate) DOWN + load stable + drones stay in lane. PRE-STEP (optional cheap): one deployed-rollout diagnostic (||v||, turn-rate, d2, speed vs [eps,overspeed] band) to confirm jumps are manifold-phase-catchup vs speed-band edges vs delay lag.
